[project]
name = "agentstudy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "bobo", email = "wdbkti@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.5",
    "pytest>=9.1.1",
]

[project.scripts]
agentstudy = "agentstudy:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
