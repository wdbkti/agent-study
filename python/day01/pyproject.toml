[project]
name = "day01"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.5",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
[tool.pytest]
pythonpath = ["."]
testpaths = ["tests"]